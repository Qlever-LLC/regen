
export const onFile = async (file: File) => {

    const verification = await fetch('https://regenscore.deno.dev/api/verify', {

        // method: 'POST',
        // send a PDF file you want to verify

    })
     
    
};